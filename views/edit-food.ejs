<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Item | Visista Bojana</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
  <style>
    body { font-family: 'Poppins', sans-serif; background: #f4f6f9; display: flex; justify-content: center; align-items: center; height: 100vh; margin: 0; }
    .form-container { background: white; padding: 40px; border-radius: 12px; box-shadow: 0 5px 20px rgba(0,0,0,0.1); width: 100%; max-width: 450px; border-top: 5px solid #3498db; }
    h2 { margin-top: 0; color: #2c3e50; text-align: center; }
    .form-group { margin-bottom: 20px; }
    label { display: block; margin-bottom: 8px; color: #666; }
    input { width: 100%; padding: 12px; border: 1px solid #ddd; border-radius: 8px; box-sizing: border-box; }
    button { width: 100%; padding: 12px; background: #3498db; color: white; border: none; border-radius: 8px; cursor: pointer; font-weight: 600; }
    .cancel { display: block; text-align: center; margin-top: 15px; color: #888; text-decoration: none; }
  </style>
</head>
<body>

  <div class="form-container">
    <h2>Edit Food Item</h2>
    <form action="/admin/edit/<%= food.id %>" method="POST">
      <div class="form-group">
        <label>Food Name</label>
        <input type="text" name="name" value="<%= food.name %>" required>
      </div>
      <div class="form-group">
        <label>Price (₹)</label>
        <input type="number" name="price" value="<%= food.price %>" required>
      </div>
      <button type="submit">Update Item</button>
      <a href="/admin/dashboard" class="cancel">Cancel</a>
    </form>
  </div>

</body>
</html>